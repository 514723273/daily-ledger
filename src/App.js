import React, { Fragment } from 'react';
import Calendar from './components/calendar';

export default function() {
    return (
        <Fragment>
            <Calendar />
        </Fragment>
    )
}